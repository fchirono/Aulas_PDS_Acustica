\documentclass[a4paper,twoside,12pt]{article}
\usepackage[top=1.5cm,left=1.5cm,right=1.5cm,bottom=2cm]{geometry}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage[brazilian]{babel}

\usepackage{graphicx}
\usepackage{subfig}
\usepackage{psfrag}
\usepackage{cite}
\usepackage[cmex10]{amsmath}
\usepackage{amssymb}
\usepackage{upgreek}
\usepackage{microtype}
\usepackage{titling}
\usepackage{courier}
\usepackage{url}
\usepackage{hyperref}
\hypersetup{
	colorlinks=true,
	linkcolor=blue,
	urlcolor=blue
}

% Use o pacote 'listings' para adicionar código e o pacote 'color' para gerar os destaques
\usepackage{listings}
\usepackage{color}
\definecolor{grey}{rgb}{0.6,0.6,0.6}
\definecolor{codegreen}{rgb}{0,0.6,0}
\definecolor{codegray}{rgb}{0.5,0.5,0.5}
\definecolor{codepurple}{rgb}{0.58,0,0.82}
\definecolor{backcolour}{rgb}{0.95,0.95,0.92}
\lstset{language=Python,				% definir para destaque de linguagem Python
	backgroundcolor=\color{backcolour},
	commentstyle=\color{codegreen},
	keywordstyle=\color{magenta},
	numberstyle=\tiny\color{codegray},
	stringstyle=\color{codepurple},
	basicstyle=\ttfamily\small,		% definir tamanho das fontes
	breaklines=true,				% definir quebra de linha automática
	linewidth=\textwidth,			% definir tamanho da caixa de código
	showstringspaces=false}			% mostrar espaços como sublinhados apenas dentro de strings


\title{\vspace{-2cm} Processamento Digital de Sinais\\ Tutorial 11 - Periodograma e Método de Welch}
\author{\url{https://github.com/fchirono/Aulas_PDS_Acustica}}

\begin{document}
	\date{}
	\maketitle
	
	\section*{Objetivo de Aprendizagem}
	Ao final desta sessão, você será capaz de:
	\begin{itemize}
		\item Estimar o espectro de uma sequência em tempo discreto usando o método do periodograma, e reconhecer suas limitações;
		\item Compreender as diferenças entre as normalizações de Espectro de Potência (PS) e Densidade Espectral de Potência (PSD), e escolher a normalização apropriada de acordo com a natureza do sinal analisado;
		\item Avaliar o efeito da duração total do sinal e o comprimento da DFT sobre a resolução em frequência e a variância das estimativas espectrais.
	\end{itemize}
	
	\begingroup
	\let\clearpage\relax
	\tableofcontents
	\endgroup
	
	% *-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-
	% *-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-
	\section{Revisão}
	\subsection{Periodograma}
	
	Seja $x[n]$, $n = 0, \ldots, N-1$ uma sequência de tempo discreto. O periodograma é a estimativa espectral obtida a partir da DFT normalizada de um único segmento (ou ``quadro'') de duração $N$ desta sequência, aplicando-se opcionalmente uma janela $w[n]$:
	
	\begin{equation}
		X_N(\Omega) = \sum_{n=0}^{N-1} w[n] x[n] e^{-j \Omega n}.
	\end{equation}
	
	O periodograma \emph{não} reduz a variância da estimativa conforme a quantidade de dados analisados aumenta: ao aumentar a duração $N$ do sinal analisado, a resolução em frequência $\Delta f = f_s / N$ melhora (i.e. diminui), mas a variância da estimativa permanece constante. Como consequência, o periodograma não converge para o espectro teórico do sinal conforme mais dados são adicionados.
	
	Esta limitação do periodograma motivou o desenvolvimento do método de Welch, que realiza a média de várias estimativas espectrais $|X_{N_{W}}|$ usando segmentos de duração $N_{W}$ menores que a duração total do sinal $N$ com sobreposição entre os segmentos, conforme descrito no Tutorial 10.
	
	% *-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-
	\subsection{Espectro de Potência (PS) e da Densidade Espectral de Potência (PSD)}
	
	Para uma sequência $x[n]$ de comprimento $N$, janelada por $w[n]$, e com fatores de normalização de janela $S_1 = \sum_{n=0}^{N-1} w[n]$ e $S_2 = \sum_{n=0}^{N-1} w^2[n]$, duas normalizações do espectro estimado (por periodograma ou Welch) são possíveis:
	
	\begin{itemize}
		\item \textbf{Espectro de Potência} (\emph{Power Spectrum}, PS): estima a \emph{potência total} contida dentro de cada ``bin'' em frequência. A PS resulta em estimativas consistentes para a potência (e portanto, para a amplitude) de sinais determinísticos (e.g. sinais senoidais), independentemente do comprimento $N$ do sinal de entrada:
		\begin{equation}
			PS(\Omega) = \frac{2}{S_1^2} \left| X_w(\Omega) \right|^2;
		\end{equation}
		
		Para um sinal $x[n]$ em Volts ($V$), o seu Espectro de Potência terá unidades $V^2$.
		
		\item \textbf{Densidade Espectral de Potência} (\emph{Power Spectral Density}, PSD): estima a \emph{densidade de potência} -- isto é, potência total dividida pela largura de banda de cada ``bin'' em frequência. A PSD resulta em estimativas consistentes para a densidade espectral de potência de sinais não-determinísticos (e.g. ruído), independentemente do comprimento $N$ usado:
		\begin{equation}
			PSD(\Omega) = \frac{2}{f_s S_2} \left| X_w(\Omega) \right|^2.
		\end{equation}
		
		Para um sinal $x[n]$ em Volts ($V$), a sua Densidade Espectral de Potência terá unidades $V^2 / Hz$.
		
	\end{itemize}
	
	Ambas as expressões definem um espectro \emph{unilateral}: definido apenas de $f=0$ a $f_s/2$, descartando as frequências negativas, e o fator ``2'' é usado para preservar a potência total do sinal.
	
	A potência de um sinal dentro de uma faixa de frequências pode ser obtida pela \emph{soma} dos valores do seu Espectro de Potência dentro desta faixa, ou pela \emph{integral} da Densidade Espectral de Potência dentro desta faixa de frequências -- isto é, levando em consideração a resolução em frequência $\Delta f$.
	
	Note que a PSD unilateral de um sinal de ruído branco com variância $\sigma^2 = 1$ é dada por $PSD(f) = 2 \sigma^2/f_s$ -- isto é, a potência $\sigma^2$ do sinal está distribuída uniformemente por toda a largura de banda $f_s$ do sinal discreto.
	
	% *-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-
	% *-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-
	\section{Tarefas}
	
	\subsection{Periodograma e Método de Welch}
	\begin{enumerate}
		\item Gere um sinal do tipo ruído branco {\tt x\_ruidobranco} com $T = 0.5$ s de duração, amostrado a $f_s = 48000$ Hz, com distribuição normal (gaussiana) de média zero e desvio padrão unitário. Filtre este sinal usando um filtro passa-faixas Butterworth de 4\textsuperscript{a} ordem, com frequências de corte $f_{c,baixa} = 50$ Hz e $f_{c,alta} = 2000$ Hz, obtendo o sinal {\tt x\_passafaixa}.
		
		O projeto de filtros em Python é assunto de um tutorial futuro; para este tutorial, siga o exemplo abaixo:
		
		\begin{lstlisting}[frame=single]
import numpy as np
import scipy.signal as ss

# cria uma instancia do Gerador de Numeros Aleatorios do modulo Numpy
rng = np.random.default_rng()

# cria uma serie temporal de numeros aleatorios com distribuicao gaussiana,
# com media 0.0, desvio padrao 1.0, e (T*fs) amostras.
x_ruidobranco = rng.normal(loc=0.0, scale=1.0, size=int(T*fs))

# Cria o filtro passa-faixas
fc_baixa = 50  		# Hz
fc_alta = 2000  	# Hz
ordem = 4

# cria filtro passa-faixa
b, a = ss.butter(ordem, [fc_baixa, fc_alta], btype='band', fs=fs)

# Filta o sinal ruido branco para obter um sinal de banda limitada
x_passafaixa = ss.lfilter(b, a, x_ruidobranco)
		\end{lstlisting}
		
		\item Escreva uma função Python {\tt periodograma(x, fs, nome\_janela, normalizacao)} que calcule o periodograma unilateral de um sinal {\tt x}, com opção de escolha da janela (use {\tt scipy.signal.get\_window()}) e da normalização (use as strings \texttt{"espectro"} ou \texttt{"densidade"} como opções). A função deve retornar o vetor de frequências {\tt f} e a estimativa espectral correspondente.
		
		\item Usando a função {\tt periodograma}, calcule a Densidade Espectral de Potência do sinal {\tt x\_passafaixa} considerando diferentes durações do sinal (por exemplo, $N$, $2 N$, $5 N$ e $10 N$ amostras, com $N = 512$). Plote as curvas obtidas em um mesmo gráfico e comente o efeito do aumento da duração do sinal sobre a resolução em frequência e sobre a variância da estimativa.
		
		\item Usando a função Python criada para o tutorial 10 como ponto de partida, escreva uma função Python {\tt welch(x, Ndft, N\_sobreposicao, nome\_janela, fs, normalizacao)} que calcule a estimativa espectral de {\tt x} pelo método de Welch, com sobreposição e janelamento configuráveis, e com opção de escolha da normalização (\texttt{"espectro"} ou \texttt{"densidade"}). A função deve retornar o vetor de frequências {\tt f} e a estimativa espectral correspondente.
		
		\item Usando a função {\tt welch}, calcule a Densidade Espectral de Potência do sinal {\tt x\_passafaixa} mantendo $N_{DFT} = 512$ e $N_{sobreposicao} = 256$ fixos, mas variando a duração total do sinal analisado (por exemplo, $N$, $2 N$, $5 N$ e $10 N$ amostras, com $N = 512$). Plote as curvas obtidas em um mesmo gráfico e compare o resultado com o obtido na Tarefa 3 usando o periodograma. O que muda em relação à resolução em frequência e à variância da estimativa?
	\end{enumerate}
	
	
	\subsection{Espectro de Potência (PS) e Densidade Espectral de Potência (PSD)}
	
	\begin{enumerate}
		\item Gere um sinal senoidal {\tt x\_senoidal} de frequência $f_0 = 1234$ Hz e amplitude RMS $V = 2 V_{rms}$, com a mesma duração e frequência de amostragem do sinal {\tt x\_passafaixa}. Some os dois sinais para obter {\tt x\_soma = x\_senoidal + x\_passafaixa}.
		
		\item Usando a função {\tt welch} com janela Hann, calcule a Densidade Espectral de Potência (PSD) do sinal {\tt x\_soma} para diferentes comprimentos de DFT (por exemplo, $N_{DFT}$, $2 N_{DFT}$, $5 N_{DFT}$ e $10 N_{DFT}$ amostras, mantendo a mesma $N_{sobreposicao}$). Plote as curvas obtidas usando a função \texttt{matplotlib.pyplot.loglog} para obter eixos log-log) e comente o comportamento do pico correspondente à senóide e do patamar correspondente ao ruído passa-faixa, conforme $N_{DFT}$ varia.
		
		\item Repita a Tarefa 7 usando agora o Espectro de Potência (PS) ao invés da PSD. Comente o comportamento do valor de pico do sinal senoidal e do patamar do ruído para os diferentes valores de $N_{DFT}$ utilizados, e compare com o resultado observado na PSD calculada na tarefa anterior. 
		
		\item Com base nos resultados obtidos, explique por que as curvas de PS e PSD calculadas nas tarefas anteriores apresentam diferentes graus de variância para diferentes valores de $N_{DFT}$.
	\end{enumerate}
	
		% -----------------------------------------------------------------------
	\section*{Referências}
	
	\begin{enumerate}
		\item K. Shin e J. Hammond, \textit{Fundamentals of Signal Processing for Sound and Vibration Engineers}. John Wiley and Sons, 2008.
		\item G. Heinzel et al, \textit{Spectrum and spectral density estimation by the Discrete Fourier transform (DFT), including a comprehensive list of window functions and some new flat-top windows}, 2002. URL: \url{https://holometer.fnal.gov/GH_FFT.pdf}
	\end{enumerate}
	
\end{document}